#ifndef VARIABLE_HPP
#define VARIABLE_HPP

#include <string>
#include <map>

#include "../Config.hpp"

using VariableName = std::string;
using VariableMap = std::map<VariableName, Value>;

#endif // VARIABLE_HPP
